# Fly.io configuration for Credit Spreads Flink Job
app = 'pipeline-credit-flink'
primary_region = 'sjc'

[build]
  dockerfile = "Dockerfile"

[env]
  KAFKA_BOOTSTRAP = "pipeline-kafka.internal:9092"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1

