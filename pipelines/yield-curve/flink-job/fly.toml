# Fly.io configuration for Yield Curve Flink Job
app = 'pipeline-yield-flink'
primary_region = 'sjc'

[build]
  dockerfile = "Dockerfile"

[env]
  KAFKA_BOOTSTRAP = "pipeline-kafka.internal:9092"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1

